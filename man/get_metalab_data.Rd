% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_metalab_data.R
\name{get_metalab_data}
\alias{get_metalab_data}
\title{Get MetaLab effect-size data}
\usage{
get_metalab_data(
  metalab_metadata = NULL,
  short_names = NULL,
  domains = NULL,
  specs = NULL,
  perform_validation = TRUE,
  version = "current"
)
}
\arguments{
\item{metalab_metadata}{Optional data frame of dataset metadata (from
\code{\link{get_metalab_metadata}} with a \code{key} column) to fetch live
from Google Sheets. If omitted, released data are read from Redivis.}

\item{short_names}{Optional character vector of dataset short names to
include.}

\item{domains}{Optional character vector of domains to include (mutually
exclusive with \code{short_names}).}

\item{specs}{Field specification (live-sheets path only); defaults to the
current spec.}

\item{perform_validation}{Whether to validate sheets before inclusion
(live-sheets path only).}

\item{version}{A MetaLab release name (e.g. \code{"2026.1"}), or \code{"current"}
(default) for the latest release (Redivis path only).}
}
\value{
A data frame (tibble) of effect sizes, or \code{NULL} (with a message)
if released data could not be fetched.
}
\description{
By default, reads a released version of the MetaLab database from Redivis
(\url{https://redivis.com/datasets/81tq-9ewzpdvz0}) — one row per effect
size across all datasets, with all coded and derived columns. Releases are
versioned and citable; the release used is announced with a message.
}
\details{
Passing \code{metalab_metadata} instead selects the legacy curator path: fetch
the live Google Sheets listed in the metadata, validate them against the
field spec, and compute effect sizes. This is how new data releases are
produced and is subject to change between calls as curators edit sheets.
}
\examples{
\dontrun{
  # released data (recommended)
  metalab_data <- get_metalab_data()
  metalab_data <- get_metalab_data(version = "2023.1")
  mutex <- get_metalab_data(short_names = "mutex")

  # live curator sheets (legacy path)
  metadata <- get_metalab_metadata(source = "sheets")
  metalab_data <- get_metalab_data(metadata)
}
}
